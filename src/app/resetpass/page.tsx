'use client'; // ถ้ามีการใช้ useState, useEffect ฯลฯ

import React from 'react';
import { Typography, Container } from '@mui/material';

export default function ResetPassPage() {
  return (
    <Container>
      <Typography variant="h4">page for Reset Password</Typography>
      {/* ฟอร์ม reset password ของคุณ */}
    </Container>
  );
}
